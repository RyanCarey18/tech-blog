<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fund My Project!</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light my-navbar">
        <div class="container-fluid">
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="my-link" aria-current="page" href="/">Home</a>
              </li>
              <li class="nav-item">
                <a class="my-link" href="/dashboard">Dashboard</a>
              </li>
              <li class="nav-item">
                        <!-- Conditionally render login or logout links -->
                {{#if logged_in}}
                <a class="my-link" id="logout">Logout</a>
                {{else}}
                <a class="my-link" href="/login">Login</a>
                {{/if}}
              </li>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <main class="container container-fluid mt-5 background">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
